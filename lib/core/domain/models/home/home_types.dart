enum HomeCardType {
  hotel,
  flight,
  car,
  package,
  destination,
  deal;
}

enum HomeSectionLayout {
  vertical,
  horizontal,
  horizontalPeek,
  grid,
  flightRecommendationList,
  verticalDealList;
}